import { DynamicQuestionDirective } from './dynamic-question.directive';

describe('DynamicQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
